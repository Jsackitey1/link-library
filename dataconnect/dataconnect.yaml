specVersion: "v1beta"
serviceId: "link-library"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "link-library-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
